# Polynomial Builder v1

## How to run

1. Install dependencies:
   pip install -r requirements.txt

2. Run the program:
   python main.py

## What it does

- Takes sample (x, y) data
- Detects polynomial degree using finite differences
- Fits polynomial using NumPy
- Plots the result